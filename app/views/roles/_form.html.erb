<%= simple_form_for(@role) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :powers %>
    <%= f.association :user %>
    <%= f.input :meta %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
